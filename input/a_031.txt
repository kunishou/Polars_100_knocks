[answer31]

df.filter(pl.col('age').is_between(10,40))


----------------------------------------------

[Tips]
・is_between(a, b)で「数値がa以上かつb以下」という条件を指定できる
・FuterWarningが表示されるため、将来的に仕様変更がある可能性あり

----------------------------------------------

[参考] pandas記法


df[df['age'].between(10,40)]